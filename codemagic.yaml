workflows:
  ios-debug-build:
    name: iOS Debug Build - No Signing
    max_build_duration: 60
    instance_type: mac_mini_m2

    environment:
      xcode: latest

    scripts:
      - name: Install XcodeGen
        script: |
          brew install xcodegen

      - name: Generate Xcode project
        script: |
          xcodegen generate

      - name: Show schemes
        script: |
          xcodebuild -list -project ThrottleartInvoiceApp.xcodeproj

      - name: Build iOS simulator app
        script: |
          xcodebuild build \
            -project ThrottleartInvoiceApp.xcodeproj \
            -scheme ThrottleartInvoiceApp \
            -sdk iphonesimulator \
            -destination 'generic/platform=iOS Simulator' \
            CODE_SIGNING_ALLOWED=NO

    artifacts:
      - ~/Library/Developer/Xcode/DerivedData/**/Build/Products/**/*.app
      - ThrottleartInvoiceApp.xcodeproj
